# ADR-005 — Explorer dimensions are separate partitions

Status: accepted

Purpose, agency, account, program activity, object class, award, and recipient classifications can overlap. The explorer may display one additive partition at a time, and every response conserves the selected parent scope using an explicit residual when necessary.

Values from different dimensions must never be summed merely because they appear on adjacent screens.
