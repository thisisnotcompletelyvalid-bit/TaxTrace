# Infrastructure

Local infrastructure is defined by the root `docker-compose.yml`:

- PostgreSQL 16;
- FastAPI service;
- Next.js web service.

This directory is reserved for a future production deployment definition (Terraform/Pulumi/Kubernetes/etc.). No cloud provider is selected in phases 0–3.
